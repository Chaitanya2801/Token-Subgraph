type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  owner: Bytes! # address
  spender: Bytes! # address
}

type TransferEntity @entity {
  id: ID!
  from: Bytes! # address
  to: Bytes! # address
}
